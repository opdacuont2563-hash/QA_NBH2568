# 🔧 แก้ไขปัญหา: ข้อมูลบันทึกแล้วแต่ไม่แสดง

## 🐛 ปัญหาที่พบ

**อาการ:**
- ข้อมูลถูกบันทึกใน Google Sheet แล้ว ✓
- แต่ Tab "ตารางแสดงข้อมูล" ไม่แสดงข้อมูล ✗

**สาเหตุที่เป็นไปได้:**
1. Department ID มี Space หรือตัวอักษรพิเศษ
2. Fiscal Year ไม่ตรงกัน (String vs Number)
3. ชื่อเดือนไม่ตรงกัน

---

## ✅ การแก้ไข

### สิ่งที่ทำ:

#### 1. เพิ่ม `.trim()` ทุกที่ที่เปรียบเทียบ String
```javascript
// เดิม
if (data[i][1] === departmentId)

// ใหม่
const deptIdStr = String(departmentId).trim();
if (String(data[i][1]).trim() === deptIdStr)
```

#### 2. เพิ่ม Debug Logging ละเอียด

**Frontend (index_v2.html):**
```javascript
console.log('=== Loading Year Data ===');
console.log('Department ID:', currentUser.id);
console.log('Fiscal Year:', fiscalYear);
console.log('Data received:', data);
```

**Backend (code_v2.gs):**
```javascript
Logger.log('========== getYearData START ==========');
Logger.log('Looking for Department ID: "' + deptIdStr + '"');
Logger.log('Row data: "' + rowDeptId + '"');
Logger.log('Found ' + foundCount + ' months');
```

#### 3. ปรับปรุง Functions ทั้งหมด:
- ✅ `getYearData()` - ดึงข้อมูลทั้งปี
- ✅ `getQAData()` - ดึงข้อมูลเดือนเดียว
- ✅ `getAllDepartmentsData()` - ดึงข้อมูลทุกแผนก
- ✅ `loadYearData()` - Frontend loader

---

## 📦 ไฟล์ที่ต้องอัพเดท

### 1. [index_v2.html](computer:///mnt/user-data/outputs/index_v2.html) (112 KB)
**การเปลี่ยนแปลง:**
- เพิ่ม Console Logging ใน `loadYearData()`
- แสดงข้อความ Alert ที่ละเอียดขึ้น
- Debug ทุก step

### 2. [code_v2.gs](computer:///mnt/user-data/outputs/code_v2.gs) (23 KB)
**การเปลี่ยนแปลง:**
- เพิ่ม `.trim()` ทุกที่เปรียบเทียบ String
- เพิ่ม Logger.log() ละเอียด
- แสดงข้อมูล row แรกๆ เพื่อ debug

---

## 🚀 วิธีติดตั้ง

### ขั้นตอนที่ 1: อัพโหลดไฟล์ (3 นาที)

```
Google Apps Script Editor:

1. อัพโหลด index_v2.html:
   - เปิดไฟล์ index_v2.html
   - Ctrl+A → Delete
   - วางโค้ดใหม่
   - Ctrl+S

2. อัพโหลด code_v2.gs:
   - เปิดไฟล์ Code.gs
   - Ctrl+A → Delete
   - วางโค้ดใหม่
   - ⚠️ แก้ SPREADSHEET_ID ให้ถูกต้อง
   - Ctrl+S
```

### ขั้นตอนที่ 2: Deploy

```
1. Deploy → Manage deployments
2. Edit → Active deployment
3. Description: "Fix data display bug v2.2.1"
4. Deploy
5. URL ยังคงเดิม
```

### ขั้นตอนที่ 3: ทดสอบและ Debug (5 นาที)

#### A. เปิด Console Log
```
1. เปิดระบบ
2. กด F12 (เปิด Developer Tools)
3. ไปที่ Tab "Console"
4. Login เข้าระบบ
```

#### B. ทดสอบโหลดข้อมูล
```
1. ไปที่ Tab "ตารางแสดงข้อมูล"
2. เลือกปีงบประมาณ 2568
3. คลิก "โหลดข้อมูล"
4. ดู Console Log
```

#### C. ตรวจสอบ Console Output

**ควรเห็น:**
```javascript
=== Loading Year Data ===
Current User: {id: "DEPT021", name: "หอผู้ป่วยหนักรวม(ICU_รวม)", ...}
Department ID: DEPT021
Fiscal Year: 2568

=== Data Received ===
Raw data: {ตุลาคม: {...}, ...}
Data keys: ["ตุลาคม"]
Number of months: 1
Month: ตุลาคม {id: "QA...", ...}
```

#### D. ตรวจสอบ Execution Log (Apps Script)

```
1. Apps Script Editor
2. เมนู "Executions" (ด้านซ้าย)
3. คลิกที่ Execution ล่าสุด
4. ดู Log
```

**ควรเห็น:**
```
========== getYearData START ==========
Looking for Department ID: "DEPT021"
Looking for Fiscal Year: "2568"
Total rows in sheet: 1
Row 1 - DeptID: "DEPT021", Year: "2568", Month: "ตุลาคม"
✓ FOUND Match at row 1: Month = "ตุลาคม"
========== getYearData RESULT ==========
Found 1 months of data
Months: ตุลาคม
========================================
```

---

## 🔍 การวินิจฉัยปัญหา

### กรณีที่ 1: Console แสดง "Number of months: 0"

**ปัญหา:** Backend ไม่เจอข้อมูล

**วิธีแก้:**

#### 1.1 เช็ค Execution Log
```
Apps Script → Executions → ดู Log

ถ้าเห็น:
"Row 1 - DeptID: "DEPT021 ", Year: "2568", Month: "ตุลาคม""
                    ↑ มี Space!

แก้ไข:
- เปิด Google Sheet
- เช็คคอลัมน์ B (Department ID)
- ลบ Space ออก
```

#### 1.2 เช็ค Department ID ไม่ตรงกัน
```
Console แสดง:
Department ID: DEPT021

Execution Log แสดง:
Row 1 - DeptID: "DEPT0021"  ← มี 0 เยอะ!

แก้ไข:
- เช็ค ID ให้ตรงกัน
- หรือแก้ในโค้ด departments array
```

#### 1.3 เช็ค Fiscal Year ไม่ตรงกัน
```
Console: Fiscal Year: 2568
Log: Row 1 - Year: "2567"  ← ผิดปี!

แก้ไข:
- เช็คข้อมูลใน Sheet คอลัมน์ D
- ตรวจสอบว่าเลือกปีถูกต้อง
```

### กรณีที่ 2: มี Error ใน Console

**Error: "Cannot read property 'id' of null"**
```
ปัญหา: currentUser เป็น null
แก้ไข: Login ใหม่อีกครั้ง
```

**Error: "google.script.run is not defined"**
```
ปัญหา: ไม่ได้ Deploy
แก้ไข: Deploy ใหม่
```

### กรณีที่ 3: Data มาแต่ไม่แสดง

**Console แสดง:**
```
Number of months: 1
Month: ตุลาคม {...}
```

**แต่หน้าจอไม่แสดง**

**วิธีแก้:**
```
1. Hard Refresh (Ctrl+Shift+R)
2. ลบ Cache
3. ลองเปิดใน Incognito Mode
```

---

## 📋 Checklist การ Debug

### ก่อนติดตั้ง:
- [ ] Backup ข้อมูลเดิม
- [ ] Backup โค้ดเดิม

### หลังติดตั้ง:
- [ ] อัพโหลด index_v2.html
- [ ] อัพโหลด code_v2.gs
- [ ] แก้ SPREADSHEET_ID
- [ ] Save ทั้งหมด
- [ ] Deploy

### การทดสอบ:
- [ ] เปิด Console (F12)
- [ ] Login เข้าระบบ
- [ ] ไปที่ Tab "ตารางแสดงข้อมูล"
- [ ] คลิก "โหลดข้อมูล"
- [ ] ดู Console Log
- [ ] ดู Execution Log
- [ ] เช็คว่าข้อมูลแสดงหรือไม่

### ถ้ายังไม่แสดง:
- [ ] Screenshot Console Log
- [ ] Screenshot Execution Log
- [ ] Screenshot Google Sheet (แถวที่มีข้อมูล)
- [ ] ส่งให้ทีม IT

---

## 🎯 คำแนะนำเพิ่มเติม

### 1. ตรวจสอบข้อมูลใน Google Sheet

**คอลัมน์ที่สำคัญ:**
- **A (ID):** QAxxxxxxxxxx
- **B (Department ID):** DEPT021 (ไม่มี Space)
- **C (Department Name):** หอผู้ป่วยหนักรวม(ICU_รวม)
- **D (Fiscal Year):** 2568 (ตัวเลข)
- **E (Month):** ตุลาคม (ชื่อเดือนภาษาไทย)

### 2. ตรวจสอบ Department ID

**ใน Code:**
```javascript
{ id: "DEPT021", name: "หอผู้ป่วยหนักรวม(ICU_รวม)", password: "ICU2568" }
```

**ใน Sheet:**
```
B2: DEPT021
```

**ต้องตรงกัน 100%** (ไม่มี Space, ตัวพิมพ์ใหญ่-เล็กตรงกัน)

### 3. ใช้ Console Log เป็นเพื่อน

```javascript
// ใน Console จะเห็น:
Current User: {id: "DEPT021", ...}  ← เช็ค ID นี้
Department ID: DEPT021              ← ต้องตรงกับ Sheet
```

```
// ใน Execution Log จะเห็น:
Row 1 - DeptID: "DEPT021"  ← เช็คว่าตรงกับ Console ไหม
```

---

## ✅ สรุป

### สิ่งที่แก้ไข:
✅ เพิ่ม `.trim()` ทุกที่เปรียบเทียบ String  
✅ เพิ่ม Console Logging ละเอียด  
✅ เพิ่ม Execution Logging ละเอียด  
✅ แสดง Alert ข้อมูลที่เป็นประโยชน์  
✅ ปรับปรุง Error Handling  

### วิธีใช้:
1. อัพโหลดไฟล์ 2 ไฟล์
2. Deploy
3. ทดสอบพร้อม Console Log
4. ดู Execution Log เพื่อ Debug

### ถ้ายังไม่แสดง:
1. ดู Console Log (F12)
2. ดู Execution Log (Apps Script)
3. เปรียบเทียบ Department ID
4. เช็คข้อมูลใน Sheet

---

## 📞 ติดต่อสอบถาม

หากทำตามขั้นตอนแล้วยังมีปัญหา:
1. Screenshot Console Log
2. Screenshot Execution Log  
3. Screenshot แถวข้อมูลใน Sheet
4. ส่งให้ทีม IT

**ข้อมูลที่ต้องการ:**
- Department ID ที่ Login
- Fiscal Year ที่เลือก
- ข้อความ Error (ถ้ามี)
- Console Log
- Execution Log

---

**พร้อมแก้ปัญหาแล้ว! 🔧**

หลังจากติดตั้งและทดสอบด้วย Console Log จะทราบสาเหตุแน่นอนครับ

---

**เวอร์ชัน:** 2.2.1  
**วันที่:** พฤศจิกายน 2568  
**Bug Fix:** แสดงข้อมูลไม่ได้  

© 2024 โรงพยาบาลหนองบัวลำภู
